source $HOME/.apikeys

# Disable Testcontainers' Ryuk reaper. Rancher Desktop's socket path
# (~/.rd/docker.sock) can't be bind-mounted back into a container, so Ryuk
# fails to start and breaks every Testcontainers-based test.
export TESTCONTAINERS_RYUK_DISABLED=true
